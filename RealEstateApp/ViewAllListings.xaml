<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RealEstateApp.ViewAllListings"
             Title="ViewAllListings">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <HorizontalStackLayout Margin="10" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Label Text="Red" FontSize="18"/>
            <Rectangle Fill="Red" />
            <Entry x:Name="PostcodeInputBox" Placeholder="Postcode" HorizontalOptions="Center"/>
        </HorizontalStackLayout>

        <VerticalStackLayout Grid.Row="2">
            <Label 
            Text="View All Listings!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

            <Label 
            Text="Row2!"
            VerticalOptions="Center" 
            HorizontalOptions="CenterAndExpand" />
        </VerticalStackLayout>
    </Grid>
    

</ContentPage>